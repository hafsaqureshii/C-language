#include <stdio.h>
int main()
{
    float a;
    printf("enter a number: ");
    scanf("%f",&a);
    if(a>0)
    {
        printf("positive number");
    }
    else if(a<0)
    {
        printf("negative number");
    }
    else
    {
        printf("zero");
    }
}
